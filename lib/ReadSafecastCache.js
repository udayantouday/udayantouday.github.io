async function ReadFile()
{
    /*
    var FileOpener = new ActiveXObject("Scripting.FileSystemObject");
    var FilePointer = FileOpener.OpenTextFile("/SafeCastCache.json", 1, true);
    var prom = FilePointer.ReadAll().then(function(filecontents) {
        return filecontents;
    });

    Promise.all([prom]).then(function(values){
        FilePointer.Close();
        return values[0];
    });
    */
    
}